import StateLoader from './StateLoader';

export default class ScaleBookStateLoader extends StateLoader {

	constructor() {
		super();
	}

	public load() {
		// check if state is in localstorage, if not load default?? etc
	}

	//private getDefaultState()

}
